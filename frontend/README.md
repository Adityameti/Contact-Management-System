
# Frontend (React + Vite)

Responsive React app to manage contacts using the provided REST API.

## Run locally
Prerequisites: **Node.js 18+**

In BASH:
cd frontend
npm install
npm run dev


The app will start at `http://localhost:5173` and expects the backend at `http://localhost:4000`.
